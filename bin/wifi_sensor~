#!/usr/bin/env python

from wifi_sensor import client_selector
import rospy

if __name__ == "__main__":
    try:
        client = client_selector.listener()
    except rospy.ROSInterruptException:
        pass
